import styled from 'styled-components'

export const Container = styled.main`
  padding: 0px 40px;
  height: 100vh;
  overflow-y: scroll; // tudo que transbordar do container, pelo eixo Y, vai ser colocado dentro de um scroll
`
